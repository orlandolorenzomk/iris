package com.iris.nexus.cluster.internal;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ClusterSettingsRepository extends JpaRepository<ClusterSettings, Boolean> {
}
